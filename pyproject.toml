[project]
name = "betternxos-mcp-server"
version = "0.1.0"
description = "Multi-device NX-OS MCP server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "pydantic>=2.12.5",
]
